import { GroupTypeEnumPipe } from './group-type-enum.pipe';

describe('GroupTypeEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTypeEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
